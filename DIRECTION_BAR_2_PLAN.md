# 红绿转向条 2.0 开发计划

## 背景结论

当前项目已经证明：左上角红绿条对 FPS 声音方向调整最有效。原因是现有非侵入式音频输入只能稳定拿到耳机最终双声道混音，左右声道能量差是最可靠的信息；前后、上下和圆环方向属于弱推断，实战价值低于红绿转向反馈。

因此下一阶段产品重点从“八方向雷达”收敛为“FPS 声音转向校准器”。

## 目标

- 强化红绿条，让玩家能快速判断应该向左还是向右转。
- 默认配置面向实战：更敏感、更醒目、更容易触发左右提示。
- 保留方向圆环作为可关闭辅助功能，但不作为核心判断。
- 继续保持非侵入式音频捕获，不接入游戏内存、不注入进程、不影响耳机正常输出。

## 默认配置调整

新默认值：

```text
Volume Gain: 2.0
Channel Separation: 2.0
Response Sensitivity: 2.0
Side Threshold: 10dB
Direction Ring: enabled
```

说明：

- `2.0` 增益、分离和灵敏度来自真实游戏测试反馈，能显著提高脚步、换弹等弱声音的可见性。
- `10dB` 纯左右阈值比之前更适合实战转向：明显偏左/右时更快进入纯左右提示。
- 如果后续测试发现误触发偏多，再通过保持时间和衰减逻辑稳定显示，而不是降低默认灵敏度。

## 实现范围

### 1. 默认设置

- 修改 `GlobalStates` 默认值：
  - `AudioSensitivity = 2.0`
  - `ChannelSeparation = 2.0`
  - `GainBoost = 2.0`
  - `DirectionSideThresholdDb = 10.0`
- 修改重置按钮逻辑，使重置回到以上实战默认值。
- 确认旧用户配置仍按本地 `settings_data.json` 保留，不强制覆盖。

### 2. 红绿转向条 2.0

- 将顶部差值条作为核心 UI 优化对象。
- 使用 dB 差值驱动条长，减少线性差值在小音量时不明显的问题。
- 增加“对准区”：
  - 左右差接近 0 时，中线区域高亮。
  - 用于提示玩家当前朝向基本对准声源。
- 增加短暂保持：
  - 声音触发后保持约 `150ms`，避免脚步声一闪而过。
- 增加平滑衰减：
  - 声音消失后 `250ms-400ms` 内淡出。
  - 避免 UI 抖动和闪烁。

### 3. 配置暴露

保留现有界面配置：

- 音量增益
- 声道分离
- 响应灵敏度
- 方向圆环开关
- 纯左右阈值

后续可新增但本轮暂不做：

- 对准区阈值
- 保持时间
- 衰减速度
- 游戏预设

## 声学原则

- 左右方向使用左右声道 RMS 能量差判断。
- 方向差异使用 dB 表达，符合人耳响度差感知。
- 红绿条表达“转向反馈”，不试图还原完整 3D 音源。
- 圆环前后方向仅作为弱提示，不作为准确定位依据。

## 验收标准

- 默认打开应用后，三个滑块均为 `2.0`。
- 纯左右阈值默认为 `10dB`。
- 左侧声音触发红色左向反馈，右侧声音触发绿色右向反馈。
- 左右接近平衡时，中线对准区有明确提示。
- 短促声音不会立即消失，视觉保持足够玩家识别。
- GitHub Actions 远端构建通过；不在本机编译。

## 发布计划

- 分支继续使用 `codex/improve-direction-visuals`。
- 完成后发布 `1.0.2` 修复/优化版本。
- Release 产物继续使用 GitHub Actions 生成。
